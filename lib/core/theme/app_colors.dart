import 'package:flutter/material.dart';

// ─── Brand / Primary ──────────────────────────────────────────────────────────
const kColorPrimaryStart  = Color(0xFF1A4FD8); // deep anchor blue
const kColorPrimaryEnd    = Color(0xFF5B9BFF); // sky blue
const kColorOnPrimary     = Color(0xFFFFFFFF);
const kColorOnPrimaryMuted= Color(0xFFD6E4FF); // faint blue tint

// ─── Backgrounds ─────────────────────────────────────────────────────────────
const kColorBgCream = Color(0xFFF7F8FC); // community / all screens
const kColorBgLight = Color(0xFFF0F4FF); // faint blue wash

// ─── Surfaces ─────────────────────────────────────────────────────────────────
const kColorCard    = Color(0xFFFFFFFF);
const kColorDivider = Color(0xFFEEF0F6);

// ─── Text ─────────────────────────────────────────────────────────────────────
const kColorTextPrimary  = Color(0xFF111827);
const kColorTextBody     = Color(0xFF374151);
const kColorTextMuted    = Color(0xFF6B7280);
const kColorTextMutedAlt = Color(0xFF9CA3AF);

// ─── Semantic ─────────────────────────────────────────────────────────────────
const kColorSosRed     = Color(0xFFEF4444);
const kColorSuccess    = Color(0xFF22C55E);
const kColorCleanDay   = Color(0xFFFFFFFF);
const kColorRelapsedDay= Color(0xFFFCA5A5);

// ─── Avatar palette (community) ───────────────────────────────────────────────
const kColorAvatarPalette = [
  Color(0xFF3B82F6),
  Color(0xFF8B5CF6),
  Color(0xFF10B981),
  Color(0xFFF59E0B),
  Color(0xFFEC4899),
  Color(0xFF06B6D4),
];
