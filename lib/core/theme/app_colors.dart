import 'package:flutter/material.dart';

// ─── Brand / Primary ──────────────────────────────────────────────────────────
const kColorPrimaryStart = Color(0xFF2563EB);
const kColorPrimaryEnd = Color(0xFF1D4ED8);
const kColorOnPrimary = Color(0xFFFFFFFF);
const kColorOnPrimaryMuted = Color(0xFFE0E7FF);

// ─── Backgrounds ─────────────────────────────────────────────────────────────
const kColorBgCream = Color(0xFFF5F3EE);   // community / warm screens
const kColorBgLight = Color(0xFFF8FAFC);   // home / cool screens

// ─── Surfaces ─────────────────────────────────────────────────────────────────
const kColorCard = Color(0xFFFFFFFF);
const kColorDivider = Color(0xFFECECEC);

// ─── Text ─────────────────────────────────────────────────────────────────────
const kColorTextPrimary = Color(0xFF1A1A1A);
const kColorTextBody = Color(0xFF4A4A4A);
const kColorTextMuted = Color(0xFF8A8A8A);
const kColorTextMutedAlt = Color(0xFF64748B);

// ─── Semantic ─────────────────────────────────────────────────────────────────
const kColorSosRed = Color(0xFFDC2626);
const kColorSuccess = Color(0xFF059669);
const kColorCleanDay = Color(0xFFFFFFFF);
const kColorRelapsedDay = Color(0xFFF87171);

// ─── Avatar palette (community) ───────────────────────────────────────────────
const kColorAvatarPalette = [
  Color(0xFF4A90D9),
  Color(0xFF7B68EE),
  Color(0xFF27AE60),
  Color(0xFFE67E22),
  Color(0xFFE91E8C),
  Color(0xFF16A085),
];
