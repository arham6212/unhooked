name: test4
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:

  flutter:
    sdk: flutter

  # iOS-style icons
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Recommended lint rules by Flutter team
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
